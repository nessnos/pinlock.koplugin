local _ = require("gettext")
return {
    fullname = _("PinLock"),
    description = _([[Adds a PIN-code lock screen, styled after typical e-reader lock screens, that can require a PIN when KOReader starts and/or when the device wakes from sleep.]]),
}
